# Пакт крови — сайт фракции демонов

Полная копия сайта **https://bloodpact-yufu.tilda.ws/** (11 страниц), собранная заново в обычный HTML
в том же оформлении: чёрный фон, белый текст, обложка на весь экран с фоновым YouTube-видео и затемнением,
линии-разделители. Адреса страниц те же, что были на Tilda: `/law/`, `/squads/`, `/ranking/` …

**Рабочий адрес: https://bloodpact.su/**

Хостинг — **GitHub Pages** (бесплатно): публикуется корень ветки `main` этого репозитория,
домен `bloodpact.su` подключён (файл `CNAME` в корне), HTTPS включён и принудителен.
Каждый коммит в `main` публикуется автоматически через 1–2 минуты.

Сайт переехал из репозитория [DeusOfSanguis/bloodpact](https://github.com/DeusOfSanguis/bloodpact):
раньше Pages публиковал папку `docs/` по адресу `deusofsanguis.github.io/bloodpact/`, теперь —
корень этого репозитория по адресу `bloodpact.su`. Старый репозиторий остаётся как архив исходников.

---

## Как править тексты

Правьте файлы в `content/` (обычный HTML), затем:

```bash
python3 build.py                  # пересоберёт сайт в корень репозитория
git add -A && git commit -m "Правки" && git push origin main
```

Заголовки обложек, видео на обложках, порядок меню и домен — в `content/pages.json`.
Посмотреть локально: `python3 -m http.server 8000` из корня репозитория → http://localhost:8000/

> ⚠️ `build.py` пишет готовый сайт прямо в корень, но удаляет только свои артефакты
> (`index.html`, `404.html`, `embed/`, каталоги страниц). `CNAME`, `.nojekyll`, `assets/img/`,
> `content/`, `scripts/`, `google-sites/` и сам `build.py` сборка не трогает.

Можно и без Python: править `*/index.html` напрямую в веб-редакторе GitHub — но тогда правки надо
дублировать в `content/`, иначе следующая сборка их перезапишет.

---

## Домен bloodpact.su

DNS у регистратора (уже настроено):

| Тип | Имя (хост) | Значение |
|---|---|---|
| A | `@` | `185.199.108.153` |
| A | `@` | `185.199.109.153` |
| A | `@` | `185.199.110.153` |
| A | `@` | `185.199.111.153` |
| CNAME | `www` | `deusofsanguis.github.io` |

Домен продублирован в `content/pages.json → "site" → "domain": "bloodpact.su"`, поэтому сборка
сама пересоздаёт `CNAME` — при пересборке он не теряется.

⚠️ **Не удаляйте `CNAME` и не меняйте поле Custom domain в Settings → Pages**, пока не готов
другой домен: Pages мгновенно начнёт переадресовывать на него все адреса, а без готового
HTTPS-сертификата сайт «ляжет» (404 / небезопасный http, фоновое видео YouTube режется как mixed content).

**Быстрый откат, если с доменом беда:** удалить `CNAME` из `main` и очистить поле
**Settings → Pages → Custom domain** — через 1–2 минуты сайт вернётся на
`deusofsanguis.github.io/bloodpact-site/`.

---

## Картинки

Старые 8 картинок уже скачаны с CDN Tilda и лежат в `assets/img/` (сайт от Tilda не зависит).
Новые (`blood-moon.jpg`, `progenitor.jpg`, `transformation.jpg`) — тоже уже в `assets/img/`.
Если понадобится перекачать старые:

```bash
python3 scripts/fetch_assets.py   # скачает файлы в assets/img/
python3 build.py                  # пересоберёт сайт с локальными картинками
```

## Видео и гифки на обложках

Обложка всегда показывает **картинку** (`cover_image` в `content/pages.json`) — она грузится всегда.
Ролик с YouTube (`youtube`) грузится **только по клику на кнопку ▶** — поэтому обложка
никогда не бывает чёрной, даже если YouTube заблокирован или не грузится.

Своё видео фоном (играет само, без звука, по кругу, грузится везде и без YouTube):

1. Положи файл `.mp4` в `assets/video/` (короткое, до 5–10 МБ — подробности в `assets/video/README.txt`).
2. В `content/pages.json` у нужной страницы впиши: `"cover_video": "имя-файла.mp4"`.
3. `python3 build.py` → коммит → пуш.

Гифка вместо картинки обложки: положи `.gif` в `assets/img/` и укажи её в `cover_image`.

---

## Что в репозитории

```
content/                исходники — единственное место, где нужно править текст
  pages.json              список страниц, заголовки/картинки/видео обложек, ID YouTube-видео, домен
  index.html              главная: секции, карточки, аккордеон, луны, иерархия, вкладки (с подсказками внутри)
  law.html, squads.html…  остальные страницы (простой HTML: <h2>, <h3>, <p>, <ul>, <hr> = разделитель)
  theme.css               оформление (цвета сайта — в блоке :root вверху)
  app.js                  скрипты: анимации, вкладки, меню, видео по клику (править не нужно)
build.py                сборка (Python 3, без зависимостей):  python3 build.py
index.html, law/, …     ГОТОВЫЙ САЙТ в корне (генерируется сборкой, публикует GitHub Pages)
404.html                страница «нет такой страницы» (генерируется сборкой)
assets/style.css        стили сайта (генерируются из content/theme.css)
assets/app.js           скрипты сайта (генерируются из content/app.js)
assets/img/             картинки сайта (cover.jpg, кровавая луна, прародитель, территории, чемпион, og-image)
assets/video/           сюда класть свои .mp4 для фонов обложек (инструкция в assets/video/README.txt)
embed/                  те же страницы без верхнего меню (если понадобится встроить в Google Sites по URL)
google-sites/           автономные HTML-файлы страниц + ASSETS.md (все ссылки на видео и картинки)
scripts/fetch_assets.py скачивание картинок с CDN Tilda
CNAME                   свой домен bloodpact.su (не удалять)
.nojekyll               отключает обработку Jekyll (не удалять)
```

| Было на Tilda | Стало | Раздел |
|---|---|---|
| `/` | `/` | Главная (Пакт крови, определения, иерархия, привилегии) |
| `/law` | `/law/` | Демонический закон |
| `/elevation` | `/elevation/` | Возвышение крови |
| `/squads` | `/squads/` | Отряды |
| `/scars` | `/scars/` | Шрамы |
| `/limits` | `/limits/` | Пределы крови |
| `/bloodbattle` | `/bloodbattle/` | Битва крови |
| `/territories` | `/territories/` | Территории (5 картинок) |
| `/incentives` | `/incentives/` | Поощрения |
| `/ranking` | `/ranking/` | Рейтинг (фото чемпиона) |
| `/court` | `/court/` | Суд |

## Если всё-таки понадобится Google Sites

Google Sites не умеет импортировать готовый сайт (ни HTML, ни ZIP). Возможны только:
**Вставка → Встроить → По URL** (адрес страницы без меню, например
`https://bloodpact.su/embed/law.html`) или **Код для встраивания** (файлы из `google-sites/`).
В обоих случаях страница показывается в рамке с прокруткой. Для «нативного» сайта на Google Sites текст удобно
копировать из файлов `google-sites/NN-*.html`, открытых в браузере (жирный шрифт и списки сохраняются),
все ссылки — в `google-sites/ASSETS.md`.

## Что отличается от Tilda

- Шрифт: вместо фирменного Tilda Sans — Inter (Google Fonts) с системным запасным шрифтом.
- Нет плашки «Made on Tilda».
- На телефонах вместо фонового видео показывается картинка обложки (так делала и Tilda).
- Тексты перенесены дословно, включая авторскую орфографию.
